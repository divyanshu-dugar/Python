name  = "Tony Stark"

# inbuild methods 
print(name.upper()) # A new string is created, Original strings doesn't convert
print(name.lower())
print(name.find('S')) # it returns the index of the element given as input
print(name.find("Tony"))
print(name.replace("Tony Stark", "Iron Man")) # Again, it does not change the original string

# Keywords are highlighted, for example print, input, etc.

print("T" in name) # To check whether a character is present in the string or not.
